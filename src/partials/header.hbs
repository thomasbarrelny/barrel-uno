		{{#with header}}
		{{#with nav}}
		<header class="header">
			<nav class="nav">
				<ul class="nav__list">
					{{#each links}}
					<li class="nav__list-item">
						<a href="{{url}}">{{title}}</a>
					</li>
					{{/each}}
				</ul>
			</nav>
		</header>
		{{/with}}
		{{/with}}
