		{{#with header}}
		{{#with nav}}
		<header class="header">
			<div class="header__container">
				<nav class="nav">
					<ul class="nav__list">
						{{#each link}}
						<li class="nav__list-item">
							<a href="{{url}}">{{title}}</a>
						</li>
						{{/each}}
					</ul>
				</nav>
			</div>
		</header>
		{{/with}}
		{{/with}}
